#!/bin/bash

# MySQL should already be installed
# ubuntu command to install: something like sudo apt-get install mysql
#

# run sql to create or update db
# run the create_db.sql to create the db

# install anything needed for the python shells
# 	possibly export variables/config file

# setup server as 
